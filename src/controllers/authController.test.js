describe('authController', () => {
  const arr = [5];
  
  beforeEach(() => {
  })
  
  beforeAll(() => {
  })
  
  afterEach(() => {
  })
  
  afterAll(() => {
  })
  
  it.skip('should ', () => {
    expect(2 + 2).toBe(4);
  });
  
  it.only('should ', () => {
    expect(2 + 2).toBe(4)
  });
  
  it.todo('should ', () => {
    expect(2 + 2).toBe(4);
  });
  
  it('should ', () => {
    expect(2 + 2).toBe(4);
  });
});